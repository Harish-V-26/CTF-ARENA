server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Add custom header with flag hint on all responses
    add_header X-Powered-By "CTF-LABS/1.0";

    location / {
        try_files $uri $uri/ =404;
    }
}
